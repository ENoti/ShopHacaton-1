<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shop"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".CreateNewCard"
            android:exported="false" />
        <activity
            android:name=".SuccessWindow"
            android:exported="false" />
        <activity
            android:name=".CardWindow"
            android:exported="false"
            android:label="@string/title_activity_card_window"
            android:theme="@style/Theme.Shop" />
        <activity
            android:name=".ProductsWindow"
            android:exported="false" />
        <activity
            android:name=".CartWindow"
            android:exported="false" />
        <activity
            android:name=".ShopWindow"
            android:exported="false" />
        <activity
            android:name=".StartMenu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>